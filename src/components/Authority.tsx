import Partners from './Partners';

const Authority = () => {
    return (
        <section className="bg-black py-12">
            <Partners />
            {/* Add more authority elements like testimonials here if needed later */}
        </section>
    );
};

export default Authority;
